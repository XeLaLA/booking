spring:
  datasource:
    url: jdbc:postgresql://localhost:5432/booking
    username: postgres
    password: terrrr
  jpa:
    show_sql: true
    format_sql: true
    hibernate:
      ddl-auto: create